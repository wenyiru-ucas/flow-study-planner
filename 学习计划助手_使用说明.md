# Flow — 学习计划助手 · 使用说明

## 🚀 启动

### 正式安装版（推荐）
1. 从 `dist/mac-arm64/` 将 `Flow学习计划助手.app` 拖入「应用程序」文件夹
2. 首次打开若提示"无法验证开发者"→ 右键 → 打开
3. 启动后在**顶部菜单栏**（右上角）出现 🍅 图标，可随时访问

### 开发模式
```bash
cd 项目目录
npx electron .
```

### 浏览器版
直接双击 `index.html` 用 Chrome 打开（功能相同，但无菜单栏托盘与原生通知）。

---

## 🍅 番茄钟

任务旁的 **🍅** 按钮开启计时（默认 25 分钟专注 + 5 分钟休息，可在设置调整）：

- 环形进度动画 + 大数字倒计时，可暂停/结束/最小化
- 专注结束自动记录学习时长，并弹出系统横幅通知（Electron 版）
- 结束后填写"完成量"更新任务进度

### ⏱ 秒表（分段计时）
番茄钟弹窗**右侧栏**，点击 **⏱ 秒表** 展开：

- 输入分段名称（如"言语理解"）→ 开始 → 每完成一段点「⏱ 分段」
- 自动生成分段列表：`01 言语理解 12分34秒`，含合计
- 番茄钟结束时秒表自动暂停，数据保留可回看

---

## 📅 月度规划

- **日历网格**：每日任务彩色圆点标记；点击某天展开当日任务
- **月导航**：◀ ▶ 切换月份，点「今天」回本月
- **历史月份**：页面底部折叠展示，可回溯完成状态
- **KPI 卡片**：进行中任务数 / 滞后数 / 今日学习指标 / 连续打卡天数

---

## 🎯 今日任务

- 任务卡片按"逾期 → 今日 → 即将开始 → 已完成"排序
- 每日指标自动锁定（每天首次加载时计算，任务延期自动重新分摊）
- 左侧栏显示**今日学习时长**与**连续打卡天数**（番茄钟结束即时刷新）

---

## 🏫 考场模拟

侧边栏「考场模拟」——模拟行测考场，**全真电子钟 + 分模块计时**：

### 电子钟
- 设置**时制**（12/24 小时制）与**开考时间**，点「▶ 开始」
- LED 红色电子钟从开考时间开始走秒，**无任何附加信息**（全真模拟）
- 开始后可**暂停 / 继续 / 结束 / 重置**；暂停时模块计时联动暂停
- 可设置**考试时长**，到时自动红色闪烁 + 系统通知 + 结束

### ⏱ 模块计时
- 预设六大模块按钮：**政治理论 / 常识判断 / 言语理解 / 数量关系 / 判断推理 / 资料分析**
- 点击模块即开始计时；切换模块自动分段；**同一模块多次自动合并**
- 右侧列表显示各模块用时 + 合计

### 📚 历史记录（自动保存）
每次模考结束**自动保存**分模块用时，可点击 **✏️** 补充：

- **试卷名称**（如"2025国考行测真题"）
- 各模块**题数 / 答对数** → 自动显示得分率百分比
- **本场总得分**
- 数据随 JSON 导入导出备份，支持单条删除

---

## 📈 数据分析

顶部切换统计周期：**📅 本周 / 📆 本月 / 📊 本年**（所有图表表头标注当前周期，时间维度图表按周期过滤）：

| 图表 | 内容 |
|------|------|
| 各科学习时长 | 环形图，周期内各任务番茄钟分钟，弧上直接标注名称+时长 |
| 标签学习时长分布 | 周期内各标签累计时长（颜色跟随标签配置） |
| 学习趋势 | 每日学习分钟数 |
| 各任务进度完成率 | 序时% vs 实际%（全程进度，不随周期变化） |
| 行测各模块正确率 | 来自**考场模拟**成绩（答对数 ÷ 题数） |
| 行测单题平均耗时 | 来自考场模拟，单位 **X分X秒/题**，红色虚线=参考用时 |
| 各模块正确率趋势 | 每次模考各模块正确率折线 |

---

## 🎨 标签颜色

- 颜色精简为 **4 色：红 / 琥珀黄 / 蓝 / 绿**（深色模式自动柔和变体）
- **设置 → 标签颜色**：为每个标签指定颜色，任务与图表自动跟随
- 默认映射：法考=红 / 考公=蓝 / 其他=绿（可自由修改）

---

## ⚙️ 设置

- **API Key**：AI 重排（DeepSeek）与番茄钟提醒需要
- **番茄钟**：专注/休息时长
- **错题标签 & 参考用时**：行测单题耗时参考线
- **数据备份与恢复**：IndexedDB 快照（自动去抖备份 + 手动快照 + 历史版本）
- **数据管理**：导出 / 导入 JSON、清空

---

## 🤖 AI 重排

点击右上角 **🤖** → 输入突发情况 → DeepSeek 分析当前进度给出重排建议（需先配置 API Key）。

---

## 🖥 Electron 增强（安装版独有）

- **菜单栏托盘**：番茄钟运行时显示实时倒计时，右键可暂停/结束/再来一组
- **原生通知**：番茄钟结束、考试到时弹出系统横幅（窗口隐藏也能收到）
- **原生菜单栏**：文件（导入导出）/ 视图（Cmd+1~4 切页面）/ 编辑 / 帮助
- **窗口隐藏常驻**：点关闭按钮隐藏到托盘，左键托盘图标恢复

---

## 💾 数据

- 数据存于应用数据目录（localStorage + IndexedDB 快照双层保险）
- 换电脑/重装：设置 → 导出 JSON → 新环境导入
- 代码安全：项目已接入 **git 版本控制**，重要改动自动存档可回退

---

## 🧪 单元测试

打开 `tests.html` 可运行核心计算逻辑测试（formatTime / KPI / 单位耗时 / 连续打卡等）。
